fn main() {
    // A vector of f32 or f64 can be sorted by vec::sort_by and PartialOrd::partial_cmp
    let mut vec = vec![1.1, 1.15, 5.5, 1.123, 2.0];

    vec.sort_by(|a, b| a.partial_cmp(b).unwrap());

    assert_eq!(vec, vec![1.1, 1.123, 1.15, 2.0, 5.5]);
}
